{% extends 'layout.html.twig' %}

{% block title %}Edit ServiceJob #{{ service_job.id }} {% endblock %}

{% block page_content %}
    <div class="row">
        <div class="col-md-4">

            <div class="card card-info">
                <div class="card-header">
                    <h3 class="card-title">Edit ServiceJob #{{ service_job.id }}</h3>
                </div>
                <div class="card-body">
                    {{ include('service_job/_form.html.twig', {'button_label': 'Update'}) }}
                    {{ include('service_job/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
