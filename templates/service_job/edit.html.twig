<!DOCTYPE html>

<title>Edit ServiceJob</title>

{% block body %}
    <h1>Edit ServiceJob</h1>

    {{ include('service_job/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('service-job.index') }}">back to list</a>

    {{ include('service_job/_delete_form.html.twig') }}
{% endblock %}
