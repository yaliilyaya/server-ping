{% extends 'layout.html.twig' %}

{% block title %}Create new ServiceCommand {% endblock %}

{% block page_content %}
    <div class="row">
        <div class="col-md-4">

            <div class="card card-info">
                <div class="card-header">
                    <h3 class="card-title">Create new ServiceCommand</h3>
                </div>
                <div class="card-body">
                    {{ include('service_command/_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
